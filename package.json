{
  "name": "parse",
  "version": "1.0.0",
  "description": "Parse data from csv file",
  "main": "index.js",
  "scripts": {
    "dev": "npx nodemon src/index.ts",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "author": "Aman",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.22",
    "rimraf": "^5.0.1"
  }
}
